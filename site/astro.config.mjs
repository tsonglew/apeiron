import { defineConfig } from "astro/config";

// GitHub Pages 项目页：tsonglew.github.io/apeiron（挂在代码仓库上）
export default defineConfig({
  site: "https://tsonglew.github.io/apeiron",
  base: "/apeiron/",
});
